<div style="max-width:1120px;margin:0 auto;padding:24px 22px 60px;">
    <div style="margin-bottom:20px;"><div style="font-size:12px;text-transform:uppercase;letter-spacing:.12em;color:var(--green);font-weight:800;">Platform admin</div><h1 style="font-size:28px;color:var(--text);margin:6px 0;">Quản lý cộng đồng</h1><p style="color:var(--text-muted);margin:0;">Duyệt creator application và theo dõi các community đang hoạt động.</p></div>
    <section class="rp-card" style="padding:22px;margin-bottom:18px;"><div style="display:flex;justify-content:space-between;align-items:center;gap:14px;flex-wrap:wrap;margin-bottom:14px;"><h2 style="font-size:18px;color:var(--text);margin:0;">Hồ sơ chờ duyệt</h2><select wire:model.live="filter" class="form-control" style="width:auto;min-width:145px;"><option value="pending">Chờ duyệt</option><option value="revision_required">Cần chỉnh sửa</option><option value="approved">Đã duyệt</option><option value="rejected">Đã từ chối</option><option value="all">Tất cả</option></select></div>@forelse($applications as $application)<div style="border-top:1px solid var(--border);padding:16px 0;display:grid;grid-template-columns:1fr auto;gap:15px;align-items:start;"><div><div style="display:flex;gap:8px;align-items:center;"><strong style="font-size:16px;color:var(--text);">{{ $application->name }}</strong><span style="font-size:11px;padding:3px 7px;border-radius:999px;background:#E1F4F7;color:#125A96;font-weight:700;">{{ $application->status }}</span></div><div style="font-size:13px;color:var(--text-muted);margin-top:4px;">/c/{{ $application->slug }} · {{ $application->applicant?->name }} · {{ $application->teaching_topic }}</div><p style="font-size:14px;line-height:1.55;color:var(--text-muted);margin:10px 0 0;">{{ $application->description }}</p></div>@if($application->status === 'pending')<div style="display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end;"><button wire:click="approve({{ $application->id }})" class="ds-btn ds-btn-primary">Duyệt</button><button wire:click="reject({{ $application->id }})" class="ds-btn">Từ chối</button></div>@endif</div>@empty<div style="padding:30px;text-align:center;color:var(--text-muted);">Không có hồ sơ trong bộ lọc này.</div>@endforelse</section>
    <section class="rp-card" style="padding:22px;"><h2 style="font-size:18px;color:var(--text);margin:0 0 14px;">Community đang hoạt động</h2><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px;">@foreach($communities as $community)<a href="{{ route('community.preview',$community->slug) }}" style="padding:15px;border:1px solid var(--border);border-radius:12px;text-decoration:none;background:#fff;"><div style="font-weight:750;color:var(--text);">{{ $community->name }} @if($community->isVerified())<span style="color:var(--green);">✓</span>@endif</div><div style="font-size:13px;color:var(--text-muted);margin-top:4px;">{{ number_format($community->users_count) }} thành viên · {{ $community->status }}</div></a>@endforeach</div></section>
</div>
