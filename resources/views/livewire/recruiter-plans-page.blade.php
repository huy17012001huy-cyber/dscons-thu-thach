<section><div class="page-heading"><span class="section-mark">Recruiter Plans</span><h1>Gói quyền liên hệ</h1><p>Mỗi gói có credit và thời hạn riêng. Giá được quản lý bởi admin, không hard-code trong giao diện.</p></div><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:1rem;">@forelse($plans as $plan)<article class="card" style="padding:1.2rem;"><h2 style="margin:0;color:#123B59;font-size:1.1rem;">{{ $plan->name }}</h2><p style="color:#61798A;font-size:.85rem;min-height:2.8rem;">{{ $plan->description }}</p><strong style="display:block;color:#125A96;font-size:1.4rem;">{{ $plan->price ? number_format($plan->price,0,',','.').'đ' : 'Theo cấu hình' }}</strong><p style="font-size:.8rem;color:#61798A;">{{ $plan->contact_credits }} credit · {{ $plan->duration_days ?: 'Không giới hạn' }} ngày</p><button wire:click="purchase({{ $plan->id }})" class="btn btn-primary">Đăng ký gói</button></article>@empty<div class="card empty-state" style="padding:2rem;">Hiện chưa có gói đang mở. Admin có thể cấu hình tại khu vực quản trị.</div>@endforelse</div><div class="card" style="padding:1rem;margin-top:1rem;"><h2 style="font-size:1rem;color:#123B59;margin:0 0 .6rem;">Lịch sử đơn hàng</h2>@forelse($orders as $order)<div style="display:flex;justify-content:space-between;padding:.55rem 0;border-bottom:1px solid #EEF2F3;font-size:.82rem;"><span>{{ $order->plan->name }}</span><span>{{ $order->status }}</span></div>@empty<p style="color:#61798A;font-size:.8rem;margin:0;">Chưa có đơn hàng.</p>@endforelse</div></section>
