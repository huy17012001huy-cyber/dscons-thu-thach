# Ngày 20 — UAT trên model gần thực tế

**Video · 75 phút.** Chọn model mô phỏng hoặc anonymize. Test happy path, empty input, missing Parameter/invalid selection. Chạy từ bộ cài đã nộp, quay kết quả, gửi FAIL cho AI bằng log an toàn, cập nhật version/dist nếu sửa và chốt giới hạn.

**Nộp:** video 2–3 phút, bảng test pass/fail và ảnh ít nhất ba tình huống. **Đạt:** bình thường, thiếu dữ liệu và lỗi người dùng đều có kết quả rõ; **chưa đạt:** chỉ test happy path.
