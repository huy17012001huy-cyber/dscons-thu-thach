# Ngày 14 — Debug build, Revit API và Hot Reload có kiểm soát

**Video · 60 phút.** Tạo lỗi an toàn ở branch/copy hoặc dùng lỗi có sẵn. Gửi AI đoạn exception/build error và stack trace liên quan, yêu cầu giải thích trước khi sửa. AI liệt kê file, build lại; học viên chạy đúng test case cũ, lưu before/after vào `bug-log.md`.

**Nộp:** video sửa lỗi, log trước/sau và ba câu reflection. **Đạt:** phân loại được lỗi môi trường/API/Transaction, biết file sửa và test lại trong Revit. **Chưa đạt:** chỉ copy lỗi hoặc kết luận pass khi chưa chạy lại.
