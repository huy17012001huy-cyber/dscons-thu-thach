APP_NAME="DSCons AI-BIM Lab"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8080
APP_LOCALE=vi
APP_FALLBACK_LOCALE=vi

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=website_thu_thach
DB_USERNAME=challenge
DB_PASSWORD=challenge_dev_only

SESSION_DRIVER=database
CACHE_STORE=database
QUEUE_CONNECTION=database
FILESYSTEM_DISK=public

# Staging must keep mail and external services disabled until its own credentials exist.
MAIL_MAILER=log
MAIL_FROM_ADDRESS="no-reply@example.test"
MAIL_FROM_NAME="${APP_NAME}"
SEED_ADMIN_EMAIL=admin@example.test
SEED_ADMIN_PASSWORD=ChangeMeBeforeStaging
BRAND_DOMAIN=localhost
BRAND_PRIMARY=#0F3D5E
BRAND_ACCENT=#DCECF7
BRAND_BACKGROUND=#F4F7F9
